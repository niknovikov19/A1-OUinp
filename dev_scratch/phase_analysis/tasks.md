1. I was playing with phase-locking analysis on a toy model. Main thing was to exclude evoked responses from phase/ower estimation. Here is the test script: `external/resp_predictor/dev_scratch/phase_lock.py`, and another one (simpler): `external/resp_predictor/dev_scratch/phase_proc_demo.py`. The results are in `external/resp_predictor/dev_scratch/artifacts/phase_lock/res_tau_2_amp_0_0_noise_0.01_seed_1111_T_15_2c80cd62`.
The main functionality is here: `external/resp_predictor/spect_proc.py`

Now I want to run similar analysis on the data from biophysical model simulation. Create `phase_lock.py` in `exp_configs/batch_rxbkg_state1_mech1/net_pulse_var_seed_f_amp`. It should read an xarray file (test: `dev_scratch/artifacts/net_pulse_var_seed_f_amp/rates_xr_combined.nc`), and run the analyses for each slice orthogonal to time. It should produce similar plots to those in `external/resp_predictor/dev_scratch/artifacts/phase_lock/res_tau_2_amp_0_0_noise_0.01_seed_1111_T_15_2c80cd62`. caching not needed for now. Keep it simple. Adhere to `AGENTS.md`, produce enough comments. Rely on `sim_data_analyzer` whenever you can. Copy `external/resp_predictor/spect_proc.py` to `analysis/phase_utils`, and anything else you need from `resp_predictor` - put there. Add an option to preprocess the signal: interp_time_outliers() from `external/sim_data_analyzer/xr_signal.py`.

The folder with the input nc-files (`dev_scratch/artifacts/net_pulse_var_seed_f_amp`) is a test one. Work there. The real folder with exp results is `exp_results/batch_rxbkg_state1_mech1/net_pulse_var_seed_f_amp/exp_L2_nseed_1_f_2_5_amp_0.01_0.03_3_t_5.0_15.0_lfp_0_300_50_ictrl_wmult_0.25_ee_0.5_pulse_IT2_d_50_c_25_r_500_0_t0_5000_jit_0` (read-only). There is `netpar` subfolder with json files. You can get the pulse timings from there "PulseSeq" field. Pulse intervals + surroundings should be set to nan before the analysis, as in `external/resp_predictor/dev_scratch/phase_lock.py`.

--------------

2. In `exp_configs/batch_rxbkg_state1_mech1`, create a new experiment `net_2pulses_var_seed_f_amps_dt0`. It should have two independently parametrized pulse sequences, e.g. PULSE_PARAMS with 2 elements. They should have the same period (1/f from batch f), same n_pulses. Amplitudes ('weight' field) come from 2 different batch params - amp1 and amp2. Another batch param is dt0 - time difference between corresponding pulses in the 2 streams. One stream starts at t0=PULSE_T0 (a constant), another one - at t0=PULSE_T0 + dt0. Think what to do when jitter is non-zero (dt0 should be constant per-pulse). Adhere to `AGENTS.md`, produce enough comments. Rely on `sim_data_analyzer` whenever you can. Copy from `exp_configs/batch_rxbkg_state1_mech1/net_pulse_var_seed_f_amp` the scripts `collect_batch_results.py` and `phase_lock.py` and adapt them for the 2-stream situation. For the phase analyysis, it's enough to use only one stream for the locking (because dt0 is constant), but time intervals should be excluded (nan-replaced for stimuli from both streams). Be sure that they are excluded for every phase analysis type you run.

